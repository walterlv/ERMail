<ContentDialog
    x:Class="Walterlv.AssembleMailing.Views.ConfigMailBoxDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Walterlv.AssembleMailing.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add an account"
    PrimaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Width="360" MinHeight="240">
            <TextBlock Margin="0 8" Text="Email address" />
            <TextBox Margin="0 0 0 12" PlaceholderText="someone@example.com" Text="{x:Bind UserName, Mode=TwoWay}" />
            <TextBlock Margin="0 8" Text="Password" />
            <PasswordBox Margin="0 0 0 12" Password="{x:Bind Password, Mode=TwoWay}" />
            <TextBlock Margin="0 -4 0 0" TextWrapping="WrapWholeWords"
                       Text="We'll save this information, so you don't have to sign in every time." />
        </StackPanel>
    </ScrollViewer>

</ContentDialog>
